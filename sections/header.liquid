{% assign main_menu_linklist = linklists[settings.menu].links %}
<div x-data="{ mobileMenuOpen: false }" class="relative bg-white">
  <div class="mx-auto px-4 sm:px-6">
    <div class="flex justify-between items-center py-6 md:justify-start md:space-x-10">
      <!-- NAME or LOGO -->
      <div class="flex justify-start lg:flex-1 truncate">
        {% if settings.use_logo %}
          {% assign logo_image = settings.logo_image | asset_url %}
          <img
            src="{{ logo_image | image_url: width: 150 }}"
            alt="{{ shop.name }} logo"
            width="{{ settings.logo_width }}"
            height="{{ settings.logo_height }}"
            class="h-8 w-auto sm:h-10"
          >
        {% else %}
          <h1 class="text-xl font-bold truncate">{{ shop.name }}</h1>
        {% endif %}
      </div>

      <!-- MOBILE HAMBURGER BUTTON -->
      <div class="-mr-2 -my-2 ml-3 md:hidden">
        <button
          x-on:click="mobileMenuOpen = true"
          type="button"
          class="bg-white p-2 inline-flex items-center justify-center focus:outline-none"
          aria-label="Open menu"
        >
          {% render 'icon-hamburger-menu', size: 5 %}
        </button>
      </div>

      <!-- NAVIGATION MENU -->
      <nav x-data="{}" class="hidden md:flex space-x-10">
        {% for link in main_menu_linklist %}
          {% if link.links != blank %}
            <div x-data="{ open: false }" class="relative">
              <button
                x-on:click="open = ! open"
                type="button"
                class="inline-flex items-center text-base text-gray-500 hover:text-gray-900"
                aria-expanded="false"
              >
                {{ link.title }}
                {% render 'icon-chevron-down', size: 3 %}
              </button>
              <div
                x-show="open"
                @click.away="open = false"
                x-transition
                class="absolute z-10 -ml-5 mt-5 transform px-2 w-screen max-w-md sm:px-0 lg:ml-0 lg:left-1/2 lg:-translate-x-1/2"
              >
                <div class="shadow-lg border ring-black ring-opacity-5 overflow-hidden rounded-lg">
                  <div class="relative grid gap-6 bg-white px-5 py-6 sm:gap-8 sm:p-8">
                    {% for sublink in link.links %}
                      <a href="{{ sublink.url }}" class="-m-3 p-3 flex items-start rounded-lg hover:bg-gray-100">
                        <div class="ml-4">
                          <p class="text-gray-900 text-base font-medium">{{ sublink.title }}</p>
                        </div>
                      </a>
                    {% endfor %}
                  </div>
                </div>
              </div>
            </div>
          {% else %}
            <a href="{{ link.url }}" class="text-base text-gray-500 hover:text-gray-900">{{ link.title }}</a>
          {% endif %}
        {% endfor %}
      </nav>

      <!-- BUTTONS -->
      <div class="hidden md:flex items-center justify-end md:flex-1 space-x-5">
        <a
          href="{{ routes.cart_url }}"
          class="whitespace-nowrap text-base text-gray-500 hover:text-gray-900"
          aria-label="Cart"
        >
          {% render 'icon-shopping-bag', size: 6 %}
        </a>
        <a
          href="{{ routes.account_login_url }}"
          class="whitespace-nowrap text-base text-gray-500 hover:text-gray-900"
          aria-label="Login"
        >
          Login
        </a>
        <a
          href="{{ routes.account_register_url }}"
          class="text-base text-gray-500 hover:text-gray-900"
          aria-label="Register"
          >Register</a
        >
      </div>
    </div>
  </div>

  <!-- MOBILE NAVIGATION MENU -->
  <div x-show="mobileMenuOpen" class="absolute z-10 top-0 inset-x-0 p-2 transform origin-top-left md:hidden">
    <div class="shadow-lg rounded-lg ring-1 ring-black ring-opacity-5 bg-white w-full">
      <div class="p-5 divide-y-2 divide-gray-50">
        <!-- MOBILE MENU HEADER TITLE PLUS CLOSE BUTTON -->
        <div class="flex items-center justify-between pb-3">
          <div>
            <h1 class="text-lg text-gray-500">{{ section.settings.mobile_menu_title }}</h1>
          </div>
          <div class="-mr-2">
            <button
              x-on:click="mobileMenuOpen = false"
              type="button"
              class="bg-white rounded-md p-2 text-gray-500"
              aria-label="Close menu"
            >
              {% render 'icon-close', size: 3 %}
            </button>
          </div>
        </div>

        <!-- NAVIGATION LINKS FOR MOBILE MENU -->
        <div class="pt-3">
          <nav class="grid gap-y-7">
            {% for link in main_menu_linklist %}
              {% assign link_handle = link.handle | replace: '-', '_' %}
              {% if link.links != blank %}
                <div x-data="{ {{ link_handle }}: false }" class="relative">
                  <button
                    x-on:click="{{ link_handle }} = ! {{ link_handle }}"
                    type="button"
                    class="text-base text-gray-500 hover:text-gray-900 flex items-center justify-between w-full"
                    aria-expanded="false"
                  >
                    <div>{{ link.title }}</div>
                    {% render 'icon-chevron-down', size: 3 %}
                  </button>

                  <div x-show="{{ link_handle }}" class="grid gap-y-7 py-3" x-transition>
                    {% for sublink in link.links %}
                      <a
                        href="{{ sublink.url }}"
                        class="-m-3 p-3 mt-2 flex items-center rounded-md text-gray-500 hover:text-gray-900 hover:bg-gray-50"
                      >
                        <span class="ml-3 text-base font-medium text-gray-500">{{ sublink.title }}</span>
                      </a>
                    {% endfor %}
                  </div>
                </div>
              {% else %}
                <a
                  href="{{ link.url }}"
                  class="-m-3 p-3 flex items-center rounded-md text-gray-500 hover:text-gray-900 hover:bg-gray-50"
                >
                  {{ link.title }}
                </a>
              {% endif %}
            {% endfor %}
          </nav>
        </div>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Header (Navbar)",
  "settings": [
    {
      "type": "text",
      "id": "mobile_menu_title",
      "label": "Title",
      "default": "Menu",
      "info": "This is the title that appears in the mobile menu."
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Header Menu",
      "default": "main-menu",
      "info": "This is the main menu that appears in the header."
    },
    {
      "type": "checkbox",
      "id": "use_logo",
      "label": "Use logo image",
      "default": false
    },
    {
      "type": "image_picker",
      "id": "logo_image",
      "label": "Logo Image",
      "info": "Upload your logo image."
    },
    {
      "type": "text",
      "id": "logo_width",
      "label": "Logo Width",
      "default": "100",
      "info": "Enter the width of the logo image in pixels."
    },
    {
      "type": "text",
      "id": "logo_height",
      "label": "Logo Height",
      "default": "50",
      "info": "Enter the height of the logo image in pixels."
    }
  ]
}
{% endschema %}
